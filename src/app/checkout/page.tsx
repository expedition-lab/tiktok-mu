export default function CheckoutSmoke() {
  return (
    <main style={{maxWidth: 800, margin: "40px auto", padding: 16}}>
      <h1>/checkout is ALIVE ✅</h1>
      <p>If you see this, routing works. Next we wire the component.</p>
    </main>
  );
}
